# Генератор Безопасных Паролей (secure-password-app)

**Secure Password App** — это настольное приложение для генерации и безопасного хранения паролей с использованием современного шифрования AES256-GCM. Все данные хранятся локально в зашифрованном виде.

## 🛡️ Особенности безопасности

- **Локальное хранение**: Все данные остаются на вашем устройстве
- **Современное шифрование**: Используется алгоритм AES256-GCM
- **Генерация надежных паролей**: Настраиваемые параметры для создания стойких паролей
- **Удобный интерфейс**: Простой и интуитивно понятный русскоязычный интерфейс

## 🏗️ Структура проекта

### Frontend (React + TypeScript)
```
src/
├── main.tsx                 # Точка входа приложения
├── App.tsx                  # Главный компонент
├── components/              # Переиспользуемые компоненты
│   └── LabelPassword.tsx   # Компонент для отображения пароля
├── pages/                   # Страницы приложения
│   ├── RootLayout.tsx      # Основной лейаут
│   ├── MainPage.tsx        # Главная страница (генератор)
│   ├── ListPasswordsPage.tsx # Страница со списком паролей
│   └── DonatePage.tsx      # Страница поддержки автора
├── utils/                   # Вспомогательные функции
│   ├── notify.ts           # Утилиты для уведомлений
│   └── utils.ts            # Общие утилиты
└── vite-env.d.ts           # Типы для Vite
```

### Backend (Rust + Tauri)
```
src-tauri/
├── src/
│   ├── main.rs            # Точка входа Rust
│   ├── lib.rs             # Основная библиотека
│   ├── schemas.rs         # Структуры данных
│   └── services/          # Сервисные функции
├── db/                    # Модуль для работы с базой данных
│   ├── Cargo.toml
│   └── src/
├── migration/             # Миграции базы данных
│   ├── Cargo.toml
│   ├── README.md
│   └── src/
├── capabilities/          # Конфигурация возможностей Tauri
│   └── default.json
├── icons/                 # Иконки приложения
├── gen/                   # Сгенерированные файлы
│   └── schemas/
└── tauri.conf.json       # Конфигурация Tauri
```

### Конфигурация и сборка
```
├── package.json          # Зависимости и скрипты (Bun)
├── tsconfig.json         # Конфигурация TypeScript
├── vite.config.ts        # Конфигурация Vite
├── src-tauri/Cargo.toml # Зависимости Rust
└── bun.lock             # Lock-файл Bun
```

## 🚀 Запуск и разработка

### Установка зависимостей
```bash
# Установка зависимостей Bun
bun install

# Установка зависимостей Rust (если не установлены)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

### Разработка
```bash
# Запуск в режиме разработки
bun tauri dev
```

### Сборка проекта

**Общая сборка:**
```bash
bun tauri build
```

**Сборка для Windows (из Linux):**

Для Microsoft Visual C++:
```bash
bun tauri build --runner cargo-xwin --target x86_64-pc-windows-msvc
```

Для MinGW-w64:
```bash
bun tauri build --runner cargo-xwin --target x86_64-pc-windows-gnu
```

## 📦 Основные зависимости

### Frontend:
- **React 19** — UI библиотека
- **React Bootstrap** — компоненты интерфейса
- **React Router** — навигация между страницами
- **React QR Code** — генерация QR-кодов
- **TypeScript** — типизация JavaScript

### Backend:
- **Tauri 2** — фреймворк для настольных приложений
- **SQLite** — локальная база данных
- **SeaORM** — для асинхронной работы с базой данных 
- **AES-GCM** — криптографические алгоритмы

### Инструменты сборки:
- **Bun** — пакетный менеджер и runtime
- **Vite** — инструмент сборки фронтенда
- **Cargo** — менеджер пакетов Rust

## 🔐 Технология шифрования

Приложение использует **AES256-GCM** — современный и рекомендуемый режим симметричного шифрования, который обеспечивает:

- **Конфиденциальность**: Надежное шифрование данных алгоритмом AES-256 с ключом длиной 256 бит
- **Аутентичность и целостность**: Режим GCM гарантирует, что данные не были изменены при хранении

## 📱 Интерфейс приложения

### Основные страницы:
1. **Главная страница** — Генератор паролей с настраиваемыми параметрами
2. **Список паролей** — Просмотр и управление сохраненными паролями
3. **Поддержка автора** — Страница для доната

### Возможности генератора:
- Настройка длины пароля
- Выбор типов символов (строчные/прописные буквы, цифры, специальные символы)
- Предварительный просмотр
- Копирование в буфер обмена
- Сохранение в зашифрованную базу данных

## 🛠️ Рекомендуемые настройки IDE

Для разработки рекомендуется использовать:

- **[VS Code](https://code.visualstudio.com/)** — легковесный и мощный редактор кода
- **[Расширение Tauri](https://marketplace.visualstudio.com/items?itemName=tauri-apps.tauri-vscode)** — официальное расширение для разработки приложений Tauri
- **[rust-analyzer](https://marketplace.visualstudio.com/items?itemName=rust-lang.rust-analyzer)** — поддержка языка Rust для работы с бэкендом Tauri
- **Rust Extension Pack** — набор расширений для разработки на Rust

### Поддержка проекта:
Если вам нравится это приложение, вы можете поддержать автора:
1. ⭐ Поставьте звезду на GitHub репозитории
2. 📢 Расскажите о приложении друзьям
3. 💰 Сделайте донат через страницу поддержки в приложении
4. 🐛 Сообщайте об ошибках и предлагайте улучшения

## 📄 Лицензия

[MIT License](LICENSE.md)
